Visor de imagenes usando Python, wxPython, OpenCV, y PIL (Python Imaging Library).

Para el curso de Visión Artificial 01-2011

NOTA: Para subir al repositorio se usan los comandos:
git commit -a
git push -u origin master
